typedef struct {
	int x, y, w;
	int winRate;
} TRacket;

typedef struct {
	float x, y;
	int ix, iy;
	float alpha;
	float vel;
} TBall;

static const int  heigth = 25, width = 65;

static const float pi = 3.1416;
